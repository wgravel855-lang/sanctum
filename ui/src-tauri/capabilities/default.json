{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Sanctum's minimal capability set. Custom commands (get_status, send_command) are allowed via the invoke handler; no plugin or filesystem permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
